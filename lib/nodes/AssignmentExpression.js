// Generated by CoffeeScript 2.0.0-beta5
void function () {
  var Expression, Identifier, leaf, LHSExpression, oneOf, OPERATORS, TYPE;
  Expression = require('../classes/Expression');
  LHSExpression = require('../classes/LHSExpression');
  Identifier = require('./Identifier');
  oneOf = require('../combinators').oneOf;
  OPERATORS = [
    '=',
    '+=',
    '-=',
    '*=',
    '/=',
    '%=',
    '<<=',
    '>>=',
    '>>>=',
    '|=',
    '^=',
    '&='
  ];
  TYPE = 'AssignmentExpression';
  leaf = function () {
    return {
      type: TYPE,
      operator: oneOf(OPERATORS),
      left: Identifier(0),
      right: Expression(0)
    };
  };
  module.exports = function (depth) {
    if (!(depth-- > 0))
      return leaf();
    return {
      type: TYPE,
      operator: oneOf(OPERATORS),
      left: LHSExpression(depth),
      right: Expression(depth)
    };
  };
}.call(this);
