// Generated by CoffeeScript 2.0.0-beta7
void function () {
  var cache$, construct, Expression, Identifier, listOf, NewExpression, Node;
  Node = require('../node');
  Expression = require('../classes/Expression');
  Identifier = require('./Identifier');
  cache$ = require('../combinators');
  construct = cache$.construct;
  listOf = cache$.listOf;
  NewExpression = function (super$) {
    extends$(NewExpression, super$);
    NewExpression.prototype.type = NewExpression.name;
    NewExpression.prototype['arguments'] = [];
    function NewExpression(depth, ancestors) {
      --depth;
      if (depth > 0) {
        ancestors = [this].concat(ancestors);
        this.callee = Expression(depth, ancestors);
        this['arguments'] = listOf([Expression])(depth, ancestors);
      } else {
        this.callee = Identifier(0);
      }
    }
    return NewExpression;
  }(Node);
  module.exports = construct(NewExpression);
  function isOwn$(o, p) {
    return {}.hasOwnProperty.call(o, p);
  }
  function extends$(child, parent) {
    for (var key in parent)
      if (isOwn$(parent, key))
        child[key] = parent[key];
    function ctor() {
      this.constructor = child;
    }
    ctor.prototype = parent.prototype;
    child.prototype = new ctor;
    child.__super__ = parent.prototype;
    return child;
  }
}.call(this);
