// Generated by CoffeeScript 2.0.0-beta7
void function () {
  var cache$, construct, Expression, maybe, Node, ReturnStatement;
  Node = require('../node');
  Expression = require('../classes/Expression');
  cache$ = require('../combinators');
  construct = cache$.construct;
  maybe = cache$.maybe;
  ReturnStatement = function (super$) {
    extends$(ReturnStatement, super$);
    ReturnStatement.prototype.type = ReturnStatement.name;
    function ReturnStatement(depth) {
      --depth;
      this.argument = maybe(Expression)(depth);
    }
    return ReturnStatement;
  }(Node);
  module.exports = construct(ReturnStatement);
  function isOwn$(o, p) {
    return {}.hasOwnProperty.call(o, p);
  }
  function extends$(child, parent) {
    for (var key in parent)
      if (isOwn$(parent, key))
        child[key] = parent[key];
    function ctor() {
      this.constructor = child;
    }
    ctor.prototype = parent.prototype;
    child.prototype = new ctor;
    child.__super__ = parent.prototype;
    return child;
  }
}.call(this);
