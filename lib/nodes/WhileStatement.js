// Generated by CoffeeScript 2.0.0-beta7
void function () {
  var construct, Expression, Node, Statement, WhileStatement;
  Node = require('../node');
  Expression = require('../classes/Expression');
  Statement = require('../classes/Statement');
  construct = require('../combinators').construct;
  WhileStatement = function (super$) {
    extends$(WhileStatement, super$);
    WhileStatement.prototype.type = WhileStatement.name;
    function WhileStatement(depth, ancestors) {
      --depth;
      ancestors = [this].concat(ancestors);
      this.test = Expression(depth, ancestors);
      this.body = Statement(depth, ancestors);
    }
    return WhileStatement;
  }(Node);
  module.exports = construct(WhileStatement);
  function isOwn$(o, p) {
    return {}.hasOwnProperty.call(o, p);
  }
  function extends$(child, parent) {
    for (var key in parent)
      if (isOwn$(parent, key))
        child[key] = parent[key];
    function ctor() {
      this.constructor = child;
    }
    ctor.prototype = parent.prototype;
    child.prototype = new ctor;
    child.__super__ = parent.prototype;
    return child;
  }
}.call(this);
